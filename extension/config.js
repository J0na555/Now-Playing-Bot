// ==============================================================
// Now Playing — YouTube watcher config
//
// Edit the two lines below BEFORE loading the extension, then
// reload it at chrome://extensions (Developer mode → Load unpacked).
// ==============================================================

// Your deployed Vercel endpoint. After deploying, this is
// https://<your-project>.vercel.app/api/push
const PUSH_URL = "<REPLACE_ME>";

// Any random string. Must match PUSH_SECRET in your Vercel environment variables.
const PUSH_SECRET = "<REPLACE_ME>";