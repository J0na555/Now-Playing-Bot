{
  "name": "telegram-now-playing",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node --import ./scripts/ts-resolve-hook.mjs --env-file-if-exists=.env --env-file-if-exists=.env.local scripts/dev-server.mjs"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@upstash/redis": "^1.38.4"
  }
}
